# Admin connection: used ONLY for migrations and the workflow worker's run-claiming.
DATABASE_URL=postgres://deedwell:CHANGE_ME@localhost:5432/deedwell
# App connection: non-superuser role, subject to Row-Level Security.
DATABASE_APP_URL=postgres://deedwell_app:CHANGE_ME@localhost:5432/deedwell
# Password assigned to the deedwell_app role at migration time.
APP_DB_PASSWORD=CHANGE_ME
# Local object-storage root (S3-compatible adapter replaces this in production).
DATA_DIR=./.data
# Model provider: "mock" (deterministic, no network) until a real adapter is configured.
MODEL_PROVIDER=mock
PORT=3000
# Google OAuth (Ad Grants wizard "Connect Google Account" step). Unset =
# the connect step shows an honest "not configured yet" state. Create a
# "Web application" OAuth client in Google Cloud Console → APIs & Services
# → Credentials, with GOOGLE_OAUTH_REDIRECT_URI added as an authorized
# redirect URI (e.g. https://coworkers.deedwell.org/v1/ad-grants/google-oauth/callback).
# Scopes requested are fixed at "openid email profile" — identity only.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:3000/v1/ad-grants/google-oauth/callback

# ---- Huddle voice -----------------------------------------------------------
# Agent voices: google (Cloud Text-to-Speech, Chirp 3 HD — production), kokoro
# (in-process open-source model), off (captions only).
VOICE_PROVIDER=off
# Speech-to-text for the mic: google (Cloud Speech-to-Text streaming —
# production), vosk (self-hosted server at STT_URL), off.
STT_PROVIDER=off
# STT_URL=ws://127.0.0.1:2700
# GOOGLE_STT_MODEL=latest_long
# GOOGLE_STT_LANGUAGE=en-US
# GOOGLE_TTS_RATE=1.0

# ---- Transactional email (Resend) --------------------------------------------
# Unset = every email is still recorded in email_outbox (status 'skipped')
# but nothing is sent — local dev and tests run this way. The key must be
# allowed to send from EMAIL_FROM's domain (deedwell.org is verified).
RESEND_API_KEY=
# EMAIL_FROM="Deedwell <hello@deedwell.org>"
# EMAIL_REPLY_TO=hello@deedwell.org
# Where internal alerts go (new signups, support requests, failures); comma-separated.
# EMAIL_OPS_TO=hello@deedwell.org
# Dashboard origin that every email links to (reset-password page lives here).
# APP_ORIGIN=https://deedwell.org
# EMAIL_LOGO_URL=https://deedwell.org/assets/logo-black.png
# Signs one-click unsubscribe links; falls back to SESSION_ENCRYPTION_KEY.
# EMAIL_UNSUBSCRIBE_SECRET=
# Worker knobs: EMAIL_WORKER=off, EMAIL_SWEEP_MS=10000, EMAIL_LOW_BALANCE_MS=3600000,
# EMAIL_LOW_BALANCE_TOKENS=250000, EMAIL_DIGEST_MS=1800000, EMAIL_DIGEST_AWAY_MINUTES=30
