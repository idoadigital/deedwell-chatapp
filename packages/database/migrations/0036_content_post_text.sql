-- Every generated design now comes with the caption to post alongside it,
-- written in the same strategy step as the image so words and picture agree.
ALTER TABLE content_assets ADD COLUMN post_text text;
